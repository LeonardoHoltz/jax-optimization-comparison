exp_name: NE_${optimizer._target_}_${dataset.name}
save_path: outputs/${exp_name}
seed: 42
hooks: []
learning_rate: 0.001
generations: 100
eval_every_gen: 10

defaults:
  - trainer: es_trainer
  - dataset: pmlb_mushroom
  - model: linear
  - optimizer: cma_es
  - loss: cross_entropy
  - _self_

hydra:
  job:
    name: ${exp_name}
  run:
    dir: outputs/${exp_name}
  # Multirun setup for seeds
  sweep:
    dir: outputs/${exp_name}
  # It is recommended to sweep parameters through the scripts or command line